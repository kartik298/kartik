export function Constraints({ id, url }) {
  // const addHandler = (event) => {
  //   event.preventDefault();
  // };

  return (
    <div className="flex flex-col gap-2">
      {/* <div className="flex flex-col">
        {solverCurrentState.type === "transient" && (
          <BCForm constraintType="initial" />
        )}
        <BCForm constraintType="dirichlet" />
      </div>
      <div className="flex justify-end gap-2">
        {/* <button
          className="text-black border-2 border-black rounded-md p-1 bg-green-500 text-2xl font-extrabold"
          onClick={addHandler}
        >
          <AiOutlinePlus />
        </button> */}
      {/* <button
          className="text-black border-2 rounded-md p-1 border-black text-2xl font-extrabold"
          onClick={closeHandler}
        >
          <AiOutlineClose />
        </button>
      </div> */}{" "}
      Work in Progress
    </div>
  );
}
